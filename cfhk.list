# 已停用（2026-09-26）：优选清单迁至 cfhk.txt——Worker 的「优选地址」直接拉它，
# 由 Worker 生成 4 节点订阅，daed 的 cf 订阅跟随 Worker 端点。
# 轮换工具：工作区 cfhk_rotate.py（探针 top4 → 写 cfhk.txt）。
# 旧工具 update_cfhk.py / cfhk.sub 一并退场（cfhk.sub 保留作历史，不再被拉取）。
